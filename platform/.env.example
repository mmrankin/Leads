# Platform admin configuration.
FLASK_SECRET_KEY=change-me
# Password for the admin screen. Leave blank to leave it open (dev only).
SETUP_PASSWORD=
# Shared SQLite DB used by both product apps (defaults to ./platform.db here).
# PLATFORM_DB_PATH=/absolute/path/to/platform.db
